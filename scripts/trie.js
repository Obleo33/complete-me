import Node from './node'

export default class Trie{
  constructor() {
    this.root = new Node();
    this.count = 0;

  }

  insert(word){
    wordArr = word.split('');
    
    wordArr.forEach( letter => {
      if (this.root.letters)
    }
  }
}
